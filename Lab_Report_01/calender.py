# Import the built-in calendar module
import calendar  

yy = int(input("Enter year: "))  

mm = int(input("Enter month: "))  

# Display the calendar for the specified month and year Using ".month()" method
print(calendar.month(yy,mm))  